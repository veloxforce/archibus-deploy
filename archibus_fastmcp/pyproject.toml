[project]
name = "archibus-fm-assistant"
version = "0.1.0"
description = "FastMCP server for Bruce BEM facility management"
requires-python = ">=3.11"
dependencies = [
    "fastmcp",
    "pydantic-settings",
    "python-dotenv",
    "requests",
]
